language: node_js
node_js: stable

cache: 
  apt: true
  directories: 
    - node_modules

before_install: 
  - npm install -g hexo-cli

# S: Build Lifecycle
install: 
  - npm install


#before_script: 
 # - npm install -g gulp

script: 
  - hexo clean
  - hexo g

after_script: 
  - cd ./public
  - git init
  - git config user.name "51228"
  - git config user.email 3051688@qq.com
  - git add .
  - git commit -m "TravisCI 自动部署"
  ## Github Pages
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches: 
  only: 
    - hexo
env: 
  global: 
   - GH_REF: git@two.github.com:51228/51228.github.io.git

notifications: 
  email: 
    - 3051688@qq.com
  on_success: change
  on_failure: always
